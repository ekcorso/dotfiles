# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
name = Emily Corso

email = ekcorso@gmail.com

[pager]
    diff = delta
    log = delta
    reflog = delta
    show = delta

[interactive]
    diffFilter = delta --color-only --features=interactive

[credential]
    helper = store

[core]
	excludesfile = $HOME/.gitignore_global
